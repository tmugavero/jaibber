# ── Jaibber Credentials ─────────────────────────────────────────────
# API key from your Jaibber org (needs messages:read/write, agents:read scopes)
JAIBBER_API_KEY=jb_...

# Jaibber server URL (default: production)
JAIBBER_API_URL=https://jaibber-server.vercel.app

# Your organization UUID (find in Admin Console)
JAIBBER_ORG_ID=

# Display name for this agent in Jaibber chat
JAIBBER_AGENT_NAME=OpenClaw

# JWT session token — required on first run for agent + webhook registration.
# Get this from the Jaibber app (Settings > copy token) or via POST /api/auth/token.
# After first run, the adapter caches the agentId/webhookId and this can be removed.
JAIBBER_SESSION_TOKEN=

# ── Project Scope ──────────────────────────────────────────────────
# Comma-separated project UUIDs to respond to, or "all" (webhook mode only).
# Poll mode requires explicit project IDs.
JAIBBER_PROJECT_IDS=all

# ── Adapter Mode ───────────────────────────────────────────────────
# "webhook" (default) — Jaibber pushes messages to this adapter.
#   Requires ADAPTER_PUBLIC_URL (use ngrok/Tailscale/Cloudflare Tunnel).
# "poll" — adapter polls Jaibber for new messages every POLL_INTERVAL_SECONDS.
#   No public URL needed. Requires explicit JAIBBER_PROJECT_IDS.
ADAPTER_MODE=webhook

# Public HTTPS URL of this adapter (required for webhook mode).
# Examples:
#   ngrok:     https://abc123.ngrok.io
#   Tailscale: https://mymachine.tail1234.ts.net
ADAPTER_PUBLIC_URL=

# Local port for the webhook receiver
ADAPTER_PORT=3456

# Poll interval in seconds (only used in poll mode)
POLL_INTERVAL_SECONDS=5

# ── OpenClaw ───────────────────────────────────────────────────────
# OpenClaw gateway URL (default: local)
OPENCLAW_URL=http://localhost:18789

# Auth token for the OpenClaw gateway.
# Find it in ~/.openclaw/openclaw.json at gateway.auth.token
# Required if gateway auth mode is "token" (the default).
OPENCLAW_AUTH_TOKEN=
